---
description: 本地启动与联调约定（路径 A/B、H2、端口、探活）
alwaysApply: true
---

# 本地启动与联调

## 优先路径

- **日常联调优先路径 B**：`SPRING_PROFILES_ACTIVE=test`（H2 **文件库**）+ 前端 Vite，无需 MySQL。
- **整站/验收用路径 A**：`docker compose`（MySQL + 后端 + 前端）。
- 不要默认裸跑 `./mvnw spring-boot:run`（会走 `local` 连 MySQL，本机无库时登录易 500）。

## 脚本入口（仓库根目录）

```bash
# 路径 A
./scripts/path-a/up.sh
./scripts/common/healthcheck-login.sh
./scripts/path-a/down.sh

# 路径 B（两个终端）
./scripts/path-b/backend.sh
./scripts/path-b/frontend.sh
```

运维说明见 `docs/运维文档/`（文件名带日期前缀）。

## H2 事实

- 路径 B（`main` 的 `test` profile）使用 **H2 文件库**，落在**仓库根** `data/h2/`（脚本注入 `H2_FILE`；yml 默认 `../data/h2/teaching_platform` 相对 `backend/`）。
- 上传根：**仓库根** `data/files/`（脚本注入 `FILE_STORAGE_ROOT`）
  - `materials/` — 课程资料（既有）
  - `lab-answers/{yyyy-MM-dd}/` — 实验答题图（本期）
- 清空本地库：停后端后删除 `data/h2/`，下次启动会重新 Flyway 灌种。
- 单元测试仍用 `src/test/resources` 的 **mem** 库，与路径 B 文件库隔离。
- 迁 MySQL 靠共用 `db/migration/`；不要假设可从 H2 文件一键导出到 MySQL。
- 禁止再把路径 B 数据落到 `backend/data/`。

## 端口与代理

- 默认后端 `8080`、前端 `5173`；路径 A/B **不要并行占同一端口**。
- 换后端端口：`SERVER_PORT=18080`；前端用 `VITE_API_TARGET=http://127.0.0.1:18080`。
- 前端开发态走 Vite `/api` 代理；`request.ts` 用相对路径 `/api/v1`，一般不必强依赖 `.env`。

## 登录探活

- 登录体必须带 `role`（`TEACHER` / `STUDENT`）。
- 探活优先 `./scripts/common/healthcheck-login.sh`（默认 `forceLogin=true`，避免 409 单点登录冲突）。
- 演示账号勿改：教师 `t9001/123456`，学生 `20260001/123456`，班级 `SE2026-1`。

## 工具链注意

- JDK **21**；`backend/mvnw` 是转调系统 `mvn` 的薄壳，本机需已安装 Maven。
