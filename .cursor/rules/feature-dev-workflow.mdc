---
description: 功能开发工作流：方案定稿 → 迭代自查 → 编码 → 迭代自查 → 验证验收
alwaysApply: true
---

# 功能开发工作流

适用于本仓库新功能、行为变更、跨前后端闭环。与 `tech-spec-decisive.mdc`（怎么写方案）、`local-dev-startup.mdc`（怎么联调）配套使用。

用户说 **「自查并修正」** 或上一轮刚写完方案/刚编码时，优先 invoke 项目技能 `.cursor/skills/self-review-and-fix/SKILL.md`（自动判定方案模式 vs 实现模式）。

## 总流程

```text
写技术方案 → 方案自查 N 轮（直至收敛）→ 编码 → 实现自查 N 轮（直至收敛）→ 构建/测试/路径 B 验收 → 交付
```

**收敛（方案与实现通用）**：每轮自查必须**列出本轮新发现问题点**并修复；**下一轮只统计「新发现」**（不含上轮已修项）。满足以下之一方可进入下一阶段：

| 条件 | 可否进入下一阶段 |
|------|------------------|
| 本轮 **新发现问题点 = 0** | ✅ 理想收敛，直接进入 |
| 本轮 **新发现问题点 ≤ 2**，且均为非阻塞项（见下）且已写入方案/修订记录 | ✅ 可收敛 |
| 本轮 **新发现问题点 > 2** | ❌ 修复后**必须**再开一轮，不得宣称完成 |
| 仍有需用户拍板的开放项 | ❌ 写入方案「待确认」并**停下编码**，不算收敛 |

**非阻塞项**（计入 ≤2 时须明确标注）：文档措辞/修订记录补全、与实现无关的示例微调；**不得**包含 API 形态、状态机、鉴权、验收不可测、决策清单缺口。

有一轮查出问题 → 修复 → **再开下一轮**；不得「修一轮就宣称完成」。

---

## 1. 方案先行（未定稿不写码）

1. 产出或更新 `docs/技术方案/YYYY-MM-DD-<主题>.md`。
2. 文首状态最终为 **已定稿（决策写死，可直接编码）**（收敛前可为草稿）。
3. 正文靠前放 **已定决策清单**（表格：决策点 → 唯一结论）；正文只保留一条实现路径。
4. 备选、否决项进附录；开放问题在定稿前闭环或标为非目标。
5. 遵守 `tech-spec-decisive.mdc`；有歧义先改方案，禁止实现阶段自行二选一。

### 1.1 方案自查 N 轮（直至收敛）

**写完技术方案后不得直接编码**；按轮次自查并修复，直到收敛。

每轮固定动作：

1. **对照** `tech-spec-decisive.mdc` 定稿自检 + 决策清单是否全覆盖（入口、API、鉴权、边界、验收、非目标）。
2. **对照现网代码**（相关 controller、页面、组件、迁移）：方案是否与仓库惯例一致、有无与现状矛盾的假设。
3. **列出本轮新发现问题点**（编号；歧义、遗漏、互斥描述、验收不可测项）；能当场写死的当场改文档。
4. **修复本轮全部问题点**（含非阻塞项）。
5. **判定收敛**（见文首收敛表）：
   - 新发现问题点 **> 2** → 进入下一轮；
   - **≤ 2** 且已处理 → 方案收敛，文首改「已定稿」，§修订记录写明轮次与本轮问题数；
   - **= 0** → 同上，理想路径。

**每轮输出格式**（对用户或 §修订记录）：

```text
方案自查第 N 轮
- 新发现问题点：K 个
  1. …
  2. …
- 已修复：…
- 结论：未收敛（K>2，继续下一轮）/ 已收敛（K≤2 或 K=0）
```

常见方案轮次会修的点：二选一未消歧、`initial-index`/异步边界未写、入口/权限含糊、与父方案未对齐、否决项未进附录、测试种子/验收口径与现网不符。

---

## 2. 编码（严格对照已收敛方案）

1. **范围**：只改方案列出的文件与能力；不做方案外的「顺手优化」。
2. **真实链路**：真实前后端 + 真实 DB/Flyway；禁止 mock 页面、假 API、静态假数据闭环。
3. **入口**：方案写死的唯一业务入口与挂载点；按方案「非目标」守口。
4. **惯例**：API 走 `request.ts`；不随意新建顶层目录；共享逻辑落方案指定路径；后端改动须有镜像测试。
5. 方案写「无改动」则不得偷偷加接口/迁移假设。

### 2.1 实现自查 N 轮（直至收敛）

**编码完成后不得直接声称可验收**；对照方案决策清单自查 N 轮并修复，直到收敛（标准同 § 总流程收敛表）。

每轮固定动作：

1. **逐项对照**方案 **决策清单**（表格一行一行勾，不是扫一眼）。
2. **扫实现高频漏项**（见下表）；对照相关文件 diff，不只靠记忆。
3. **列出本轮新发现问题点**（编号；方案偏差、实现 bug、测试缺口、注释/JavaDoc 与行为不一致等）。
4. **修复本轮全部问题点**（方案缺口 → 先回写方案再改码；实现 bug → 最小 diff）。
5. **判定收敛**（见文首收敛表）：
   - 新发现问题点 **> 2** → 下一轮；
   - **≤ 2** 且已修复 → 实现收敛，进入 §3 验证；
   - **= 0** → 理想收敛，进入 §3。

**每轮输出格式**：

```text
实现自查第 N 轮
- 新发现问题点：K 个
  1. …
  2. …
- 已修复：…
- 结论：未收敛 / 已收敛
```

| 类别 | 自查点 |
|------|--------|
| 契约 | `/api/v1`、响应解包、枚举/状态与后端一致 |
| 前端交互 | 方案写死的 props/事件、只读/编辑态、双根模板等 |
| 异步与竞态 | 切题/切路由：关弹层、scope 快照、`generation`/`session` 丢过期请求 |
| 资源生命周期 | blob/预览：先 `closePreview` 再 revoke；增量缓存勿全量闪烁 |
| 边界 | 加载失败是否静默、空列表、并发重复 fetch、卸载中途回调 |
| 范围外 | 未开通能力（如未传 `enable-images`）语义不变 |
| 测试 | 方案 §测试计划用例名/断言是否落地；错误文案是否与方案一致 |

常见实现轮次会修的点：`teleported` 遗漏、computed 未 `unref`、预览重挂载与 `initial-index`、竞态打开旧题预览、失败标记 `''` 阻断重试、Controller/JavaDoc 仍写旧语义。

---

## 3. 验证（收敛后必做，声称完成前须有证据）

```bash
cd frontend && npm run build          # 前端有改动
cd backend && ./mvnw test             # 后端有改动
./scripts/path-b/backend.sh           # 终端 1
./scripts/path-b/frontend.sh          # 终端 2
./scripts/common/healthcheck-login.sh
```

手工验收对照方案 **§ 验收标准**（演示账号：教师 `t9001/123456`、学生 `20260001/123456`）。验证失败 → 回到 **§2.1 实现自查**（新发现问题点重新计数），不得跳过收敛直接改一句就交差。

---

## 4. 交付

- 未明确要求不 commit / push / PR。
- 提交时消息写「为什么」；方案与代码同一功能一并纳入（除非用户只要代码）。

## 反例

- ❌ 方案只写一版、未迭代自查就标「已定稿」开写码
- ❌ 方案/实现只自查一轮，或用户催就停在「本轮仍发现 >2 个新问题」
- ❌ 本轮新发现问题点 > 2 却宣称已收敛
- ❌ 编码完不列「本轮新发现问题点」就口头说对照过了
- ❌ 验证失败不回到实现自查，直接口头说「可验收」
- ❌ 为通过自查硬改方案口径而不改实现
