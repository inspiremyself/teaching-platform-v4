---
description: 技术方案必须写死决策、禁止未决二选一，以便无歧义实现
globs: docs/技术方案/**/*
alwaysApply: false
---

# 技术方案：决策必须写死

技术方案是**可执行规格**，不是调研备忘。写完后 Cursor / 实现者应能直接编码，无需再猜「选 A 还是 B」。

## 硬性要求

1. **每个设计点只留一个结论**：存储、API 形态、字段主真相、依赖/库、路由入口、页面挂载点、鉴权方式、验收分期等，一律写死。
2. **禁止未决二选一**：不得出现「A 或 B」「二选一即可」「推荐…也可…」「实施时再定」「视情况」「待确认」且不给唯一答案。
3. **可选字段 ≠ 可选方案**：协议里字段可标必填/选填；「做不做 / 用哪种实现 / 挂哪个入口」不能悬空。
4. **备选方案只能进附录**：否决项放「已否决方案」并写明原因；正文只保留选定方案。
5. **开放问题必须闭环**：Open Questions 定稿前要么写成已决策条目，要么移入「非目标」并标明所属 Phase。
6. **实现时遇歧义先停**：对照正文仍有互斥路径时，先改技术方案写死，再改代码；禁止在 PR 里自行挑一边。
7. **入口唯一**：每个新页面/能力只写一条导航入口；禁止「可挂在 A 或 B」。
8. **权限对齐现网**：新鉴权（如下载/预览）须写明跟哪条现有能力一致（如报告可见性），禁止含糊写「教师」或只绑 `created_by` 而不说明例外。
9. **双源字段写清胜者**：请求体两处可表达同一语义时（如 `answerText` vs `payload.text`），写死以谁为准、是否 400、是否 canonicalize。
10. **验收与分期一致**：文首「已定稿」时，验收条目必须能映射到明确 Phase；禁止 Phase 1 未交付却写在总验收里不拆档。

## 定稿结构（建议）

1. 文首：状态 = `已定稿（决策写死，可直接编码）`
2. 靠前：**已定决策清单**（表格：决策点 → 唯一结论）
3. 正文只描述选定路径
4. 附录：**已否决方案**（否决项 + 原因）
5. 修订记录写明本次消歧内容

## 写法模板

- ✅「后端使用 Thumbnailator 压缩，封装为 `LabAnswerImageCompressor`。」
- ✅「预览接口一期采用 `GET .../answer-images?path=`；前端用带 Token 的 request 拉 blob 再 `createObjectURL`。」
- ✅「教师实验总览入口仅挂 `LabReports`；不新增顶栏一级菜单；不从 `Labs` 进入。」
- ✅「以 `answerText` 为准写入并 canonicalize `payload.text`；不一致不 400。」
- ✅「验收拆 Phase 1（§12.A）/ Phase 2（§12.B）；总览矩阵等属 Phase 2。」
- ❌「前端 canvas 或 `browser-image-compression`，实施时二选一即可。」
- ❌「`answer_file_path` 可继续空置或可选冗余首图路径。」
- ❌「入口可挂在 `Labs` 或 `LabReports`。」
- ❌「调用者是学生本人或该实验教师」（未说明是否硬绑 `created_by`、是否对齐报告权限）。

## 定稿自检（写完扫一遍）

- [ ] 全文搜索：`或` / `二选一` / `可选方案` / `推荐` / `待定` / `亦可` / `可挂在` / `可不写` — 凡涉及实现路径的，改成唯一结论
- [ ] 每个 API / 表字段 / 页面入口 / 压缩库 / 鉴权边界只有一条选定描述
- [ ] 「非目标」已覆盖明确不做的备选，并标清归属 Phase（若分期）
- [ ] 文首状态为「已定稿」时，正文与决策清单不得再留互斥选项
- [ ] 验收标准与实施分期一一对应，无「未做 Phase 却出现在总验收」的混写

## 审阅技术方案时（给决策者）

若发现歧义：列出**歧义点、含义、推荐答案**，等用户确认后再回写文档；禁止带着未决二选一进入实现。
